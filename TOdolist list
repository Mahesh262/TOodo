import React from 'react';

const Store = ({ todos, delhand }) => {
    return <>
        {todos.map((todos, index) => {
            return <h5 key={index}>{todos} &nbsp;<button className="btn btn-success" onClick={() => delhand(index)}>Delete</button></h5>
        })}
    </>;
};

export default Store;
